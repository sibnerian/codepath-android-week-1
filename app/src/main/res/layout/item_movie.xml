<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingBottom="@dimen/movie_item_vertical_padding"
    android:paddingTop="@dimen/movie_item_vertical_padding"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageView
        android:scaleType="centerInside"
        android:layout_width="@dimen/poster_width"
        android:layout_height="@dimen/poster_height"
        android:layout_marginRight="@dimen/movie_poster_right_margin"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="@color/colorPrimary"
        android:id="@+id/moviePosterImageView" />

    <TextView
        android:textSize="@dimen/title_text_size"
        android:layout_width="wrap_content"
        android:textColor="@color/textColor"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/moviePosterImageView"
        android:layout_toEndOf="@+id/moviePosterImageView"
        android:id="@+id/movieTitleTextView" />

    <TextView
        android:textSize="@dimen/text_size"
        android:textColor="@color/textColor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/movieTitleTextView"
        android:layout_toRightOf="@+id/moviePosterImageView"
        android:layout_toEndOf="@+id/moviePosterImageView"
        android:id="@+id/movieOverviewTextView" />
</RelativeLayout>